# Deployed JobFit environment. Read at BUILD time — after editing, run
# `npm run build` and hit "Reload" on the extension in chrome://extensions.
# `host_permissions` in the manifest is derived from these origins automatically.

VITE_API_URL=https://jobfit-backend-7g3b6oc3ja-an.a.run.app/api/v1
VITE_WEB_URL=https://jobfit-frontend-six.vercel.app
